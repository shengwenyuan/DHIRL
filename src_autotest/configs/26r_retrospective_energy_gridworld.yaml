# Recreated submitted Gridworld configuration with test-only action normalization.
provenance:
  purpose: formal_rebuttal
  baseline_tag: 26r-p0-baseline-grid-lab-20260726

defaults:
  task: gridworld
  ll_filename: metrics.csv
  output_dir: src_autotest/outputs/26r_retrospective_energy_v1
  data_dir: /home/swy/U-Frei/IRL/DHIRL/data/gridworld
  num_repeats: 1
  num_latents: 2
  gate_mode: retrospective
  retrospective_score_mode: action_normalized
  rand_seed: 42
  loss_threshold: 0.001
  max_iterations: 100

groups:
  grid_retrospective_action_normalized_s42:
    id: grid_retrospective_action_normalized_s42
    experiments:
      - id: E00
